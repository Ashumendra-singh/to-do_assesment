# To-Do
A fullstack website for task management

# Setup Guide for frontend
1. Install dependencies:
   run - `npm install`
2. Start frontEnd:
    run - `npm run dev`

# Setup Guide for backend
1. Install dependencies:
    run - `npm install`
2. Add all credentials on .envfile
3. Start backend:
    run - `npm run dev`



